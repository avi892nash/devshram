#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Get the commit message source
COMMIT_SOURCE=$2

# Only run for interactive commits (not merge, squash, etc.)
if [ -z "$COMMIT_SOURCE" ] || [ "$COMMIT_SOURCE" = "message" ]; then
  exec < /dev/tty && npx cz --hook || true
fi
